// @flow

// Action types
export type ActionType = { type: string, payload: Object }

// Service types
export type LocationType = { latitude: number, longitude: number }

// Route types
export type NavigationRouteType = { key: string, title?: string, props?: Object }
